# tsx

> transpile TypeScript source files with tsx

### Command

```
readme-assert --import tsx --main ./src/index.ts
```

```ts test
import { pow2 } from '@laat/tsx';
const a: number = pow2(2);
a; //=> 4
const b: number = pow2(4);
b; //=> 16
```
