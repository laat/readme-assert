# swc

> transpile TypeScript source files with SWC

### Command

```
readme-assert --import @swc-node/register/esm-register --main ./src/index.ts
```

```ts test
import { pow2 } from '@laat/swc';
const a: number = pow2(2);
a; //=> 4
const b: number = pow2(4);
b; //=> 16
```
