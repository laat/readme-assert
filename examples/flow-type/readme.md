# flow-type

> can read local babelrc for transforms

### Command

```
readme-assert --babel
```

````
```js test
function foo(one: any, two: number, three?): string {
  return "hello flow";
}
foo(); //=> 'hello flow'
```
````

```js test
function foo(one: any, two: number, three?): string {
  return "hello flow";
}
foo(); //=> 'hello flow'
```
